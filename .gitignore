# ─────────────────────────────────────────────────────────────
#  Nicht ins (öffentliche) Repo / nicht über GitHub Pages ausliefern
# ─────────────────────────────────────────────────────────────

# PHP-Backend: gehört ausschließlich auf die Subdomain api.mindshift-ki.de,
# nicht auf GitHub Pages (dort kein PHP) und nicht ins öffentliche Repo.
api/

# Roh-/Quellbilder: nur die optimierten Bilder in bilder/ werden ausgeliefert.
Bilder und Auszeichnungen/

# Interne Dokumentation (nicht öffentlich ausliefern)
DEPLOYMENT.md

# Lokale Tooling-/Editor-/Preview-Dateien
.claude/
.vscode/
.idea/

# Betriebssystem-Müll
.DS_Store
Thumbs.db
desktop.ini

# Arbeits-/Backup-Dateien (nie ausliefern, nie ins Repo)
*.bak
*.bak-*

# Ungenutzte Quell-/Arbeitsbilder (nicht auf der Live-Site referenziert)
bilder/Speaker-Slam.png
bilder/Website - *.jpg
